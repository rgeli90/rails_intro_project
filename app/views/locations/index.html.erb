<div class="container">
    <h1>Regions</h1>
    <div class="row">
        <% @locations.each do |location| %>
            <div class="col-md-3">
                <div class="card mb-3">
                    <div class="card-body">
                        <%= link_to location.name, location %>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</div>
